let name = "JavaScript";

for (let char of name) {
    console.log("Character: " + char); // Iterates over all characters in "JavaScript" and prints each one on a new line, preceded by "Character: "
}

